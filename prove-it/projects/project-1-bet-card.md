# Project 1 bet card

First action I expect: Read the fixed contract, rider, and protected test, then propose a plan without editing.

Earliest signal I'd interrupt on: A plan to inspect or copy a real session, backfill legacy records, or obtain a missing snapshot from the current seed.

The fact only I know: GAiDFLY sessions are evidence of student learning and judgment; missing historical provenance must remain unknown rather than be reconstructed.

Cancel trigger: The same false premise survives two corrections.
